name = "counter-api"
main = "src/worker.ts"
compatibility_date = "2024-01-01"

# Set Upstash Redis credentials as secrets:
#   wrangler secret put UPSTASH_URL
#   wrangler secret put UPSTASH_TOKEN
# For local dev, add them to a .dev.vars file (gitignored).
